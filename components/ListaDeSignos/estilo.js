import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
    backgroundColor: '#fff',
  },
 lista: {
   padding: 10,
   marginBottom: 60,
 }
});

export default estilo;